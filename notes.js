console.log("file notes.js is loaded");

var age = 30000000000000;

// const addNumbers = (a, b) => a + b;

const addNumbers = (a, b) => a+b;

module.exports = {
    age,
    addNumbers,
}; // export the variable age